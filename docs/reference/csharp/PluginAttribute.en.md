# PluginAttribute

`sealed class`

Attribute describing plugin metadata. The plugin name must contain only lowercase letters, numbers and underscores (Endstone requirement).

**Namespace** `Endstone.Loader`

**Inheritance** `System.Attribute` › `object`

## Constructors

- `PluginAttribute(string name, string version)` Attribute describing plugin metadata. The plugin name must contain only lowercase letters, numbers and underscores (Endstone requirement).

## Properties

### `Authors` : `string[]`

`{ get;set; }`

### `Description` : `string`

`{ get;set; }`

### `Name` : `string`

`{ get; }`

### `Version` : `string`

`{ get; }`

